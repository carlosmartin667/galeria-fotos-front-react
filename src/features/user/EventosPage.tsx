import { useQuery } from '@tanstack/react-query';
import { routes } from '@/config/routes'; import { queryKeys } from '@/services/api/queryKeys'; import { getEventos } from '@/services/eventos/eventosApi'; import { EmptyState } from '@/shared/components/EmptyState'; import { LoadingState } from '@/shared/components/LoadingState'; import { Seo } from '@/shared/components/Seo'; import { UserCard } from './components/UserList';
export function EventosPage(){ const q=useQuery({queryKey:queryKeys.eventos.list(),queryFn:getEventos}); if(q.isLoading)return <LoadingState/>; return <section><Seo title="Eventos" description="Eventos disponibles"/><h1 className="h3">Eventos</h1>{q.data?.length===0?<EmptyState title="Sin eventos"/>:null}<div className="row g-3">{(q.data??[]).map(e=><div className="col-md-4" key={e.id}><UserCard title={e.nombre} description={e.fechaEventoUtc||e.estado} to={routes.user.eventoDetail(e.id)}/></div>)}</div></section>}
