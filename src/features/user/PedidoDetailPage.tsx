import { useParams } from 'react-router-dom'; import { useQuery } from '@tanstack/react-query'; import { queryKeys } from '@/services/api/queryKeys'; import { getPedido } from '@/services/pedidos/pedidosApi'; import { EmptyState } from '@/shared/components/EmptyState'; import { LoadingState } from '@/shared/components/LoadingState'; import { Seo } from '@/shared/components/Seo';
export function PedidoDetailPage(){ const {id=''}=useParams(); const q=useQuery({queryKey:queryKeys.pedidos.detail(id),queryFn:()=>getPedido(id)}); if(q.isLoading)return <LoadingState/>; if(!q.data)return <EmptyState title="Pedido no encontrado"/>; return <section><Seo title="Pedido" description="Detalle pedido"/><h1 className="h3">Pedido {q.data.numero||q.data.id}</h1><p>Estado: {q.data.estado}</p><p>Subtotal ${q.data.subtotal??0} - Descuento ${q.data.descuento??0} - Total ${q.data.total??0}</p><p>Cupon: {q.data.cuponCodigo??'-'}</p>{(q.data.items??[]).map(i=><div key={i.id}>{i.nombre} ${i.subtotal??0}</div>)}</section>}
