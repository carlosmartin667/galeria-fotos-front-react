import { useQuery } from '@tanstack/react-query'; import { routes } from '@/config/routes'; import { queryKeys } from '@/services/api/queryKeys'; import { getPedidos } from '@/services/pedidos/pedidosApi'; import { EmptyState } from '@/shared/components/EmptyState'; import { LoadingState } from '@/shared/components/LoadingState'; import { Seo } from '@/shared/components/Seo'; import { UserCard } from './components/UserList';
export function PedidosPage(){ const q=useQuery({queryKey:queryKeys.pedidos.list(),queryFn:getPedidos}); if(q.isLoading)return <LoadingState/>; return <section><Seo title="Pedidos" description="Mis pedidos"/><h1 className="h3">Mis pedidos</h1>{q.data?.length===0?<EmptyState title="Sin pedidos"/>:null}<div className="row g-3">{(q.data??[]).map(p=><div className="col-md-4" key={p.id}><UserCard title={p.numero||p.id} description={`${p.estado??''} Total $${p.total??0} Cupon ${p.cuponCodigo??'-'}`} to={routes.user.pedidoDetail(p.id)}/></div>)}</div></section>}
