import { useMutation } from '@tanstack/react-query';
import { createFotoMetadataBulkAdmin, createStorageKeysBulkAdmin } from '@/services/admin/fotosAdminApi';
import { importarFotosPexelsAdmin } from '@/services/admin/pexelsApi';
import { AdminPageHeader } from '@/shared/components/admin/AdminPrimitives';
export function AdminFotosToolsPage({ mode }: { mode: 'bulk'|'metadata'|'pexels' }){const bulk=useMutation({mutationFn:()=>createFotoMetadataBulkAdmin({fotos:[{nombreArchivo:'x.jpg'}]})});const keys=useMutation({mutationFn:()=>createStorageKeysBulkAdmin({eventoId:'ev-1',nombresArchivo:['x.jpg']})});const pexels=useMutation({mutationFn:()=>importarFotosPexelsAdmin({eventoId:'ev-1',query:'wedding',cantidad:3})});return <section><AdminPageHeader title={mode==='pexels'?'Importar Pexels':mode==='metadata'?'Metadata fotos':'Bulk fotos'}/><button className="btn btn-dark me-2" onClick={()=>bulk.mutate()}>Procesar bulk</button><button className="btn btn-outline-dark me-2" onClick={()=>keys.mutate()}>Generar storage keys</button>{mode==='pexels'?<button className="btn btn-outline-secondary" onClick={()=>pexels.mutate()}>Importar</button>:null}</section>}
